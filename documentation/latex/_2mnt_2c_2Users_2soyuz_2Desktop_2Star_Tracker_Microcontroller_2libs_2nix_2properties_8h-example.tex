\hypertarget{_2mnt_2c_2Users_2soyuz_2Desktop_2Star_Tracker_Microcontroller_2libs_2nix_2properties_8h-example}{}\section{/mnt/c/\+Users/soyuz/\+Desktop/\+Star\+\_\+\+Tracker\+\_\+\+Microcontroller/libs/nix/properties.\+h}
Reads config files, reduces the number of command line arguments. All lines with \char`\"{}name\char`\"{} = value will be accepted. All spaces and tabs will be removed before reading. All lines that are invalid are ignored, otherwise you can use \# to comment. The variables will be read as a string, when you try and access it, it will convert it to the requested datatype.

\begin{DoxyVerb}Properties p;                           // Creates the object.
p.ReadFile("config.properties");        // Reads in the variables from the file.
int a = p.GetInteger("variable_int");   // a now is what "variable_int" was.

Sample file (config.properites):
#This is a config file
variable_int        = 1 # This is a valid line.
vari a ble = 20         # This is also a valid line "variable = 20".
ar=afv=ew               # This line will be ignored.
\end{DoxyVerb}


\begin{DoxyAuthor}{Author}
Tom Creusot
\end{DoxyAuthor}

\begin{DoxyCodeInclude}
\textcolor{comment}{/*}
\textcolor{comment}{ *  Reads config files, reduces the number of command line arguments.}
\textcolor{comment}{ *  @file   properties.h}
\textcolor{comment}{ *  @author Tom Creusot}
\textcolor{comment}{ */}

\textcolor{preprocessor}{#pragma once}
\textcolor{preprocessor}{#include <map>}
\textcolor{preprocessor}{#include <string>}
\textcolor{preprocessor}{#include <iostream>}
\textcolor{preprocessor}{#include <fstream>}

\textcolor{preprocessor}{#include "\hyperlink{util_8h}{libs/util/util.h}"}

\textcolor{keyword}{using namespace }\hyperlink{namespacestd}{std};
\textcolor{keyword}{using namespace }\hyperlink{namespaceutil}{util};

\textcolor{keyword}{namespace }\hyperlink{namespacenix}{nix}
\{

\textcolor{keyword}{class }Properties
\{
\textcolor{keyword}{protected}:
    map<string, string> hash;

\textcolor{keyword}{public}:
    \textcolor{keywordtype}{void} ReadFile       ( \textcolor{keywordtype}{string} file );


    \textcolor{keywordtype}{void} Add            ( \textcolor{keywordtype}{string} name, \textcolor{keywordtype}{string} value );


    \textcolor{keywordtype}{int} GetInteger      ( \textcolor{keywordtype}{string} name );


    decimal GetDecimal  ( \textcolor{keywordtype}{string} name );


    \textcolor{keywordtype}{string} GetString    ( \textcolor{keywordtype}{string} name );

    \textcolor{keywordtype}{string} GetFileString ( \textcolor{keywordtype}{string} name );

    \textcolor{keyword}{static} \textcolor{keywordtype}{void} RemoveAfterComment ( \textcolor{keywordtype}{string}* line );


    \textcolor{keyword}{static} \textcolor{keywordtype}{void} RemoveTabsSpaces ( \textcolor{keywordtype}{string}* line);


    \textcolor{keyword}{static} \textcolor{keywordtype}{bool} SeparateNameValue ( \textcolor{keywordtype}{string}& line, \textcolor{keywordtype}{string}* name, \textcolor{keywordtype}{string}* value );
\};
\}
\end{DoxyCodeInclude}
 